# SYNTHEX AI STUDIO 依賴（第十輪更新）

# ── 核心 ─────────────────────────────────────────────────────────
anthropic>=0.50.0            # Claude API SDK（Opus 4.6/Sonnet 4.6 支援）

# ── 設定管理（config.py 必要）────────────────────────────────────
pydantic-settings>=2.3.0     # SynthexConfig 型別驗證 + .env 讀取
pydantic>=2.7.0              # pydantic-settings 依賴

# ── 結構化日誌（logging_setup.py 必要）──────────────────────────
structlog>=24.1.0            # 生產級結構化日誌（JSON 輸出）

# ── 觀測性（observability.py，選填）────────────────────────────
# pip install opentelemetry-api opentelemetry-sdk
# opentelemetry-api>=1.24.0
# opentelemetry-sdk>=1.24.0

# ── 瀏覽器 QA（選填）────────────────────────────────────────────
playwright>=1.40.0           # 安裝後執行：playwright install chromium

# ── Project Brain v1.1 ────────────────────────────────────────────
# 向量記憶：語義搜尋（選填，未安裝時自動降級到 SQLite FTS5）
chromadb>=1.5.5              # pip install chromadb

# MCP Server：讓 Claude Code 直接呼叫 Project Brain
mcp>=1.26.0                  # pip install mcp

# ── 安裝指令 ─────────────────────────────────────────────────────
# 完整安裝（含向量記憶和 MCP）：
#   pip install -r requirements.txt
#
# 最小安裝（只有核心功能）：
#   pip install anthropic
#
# Project Brain MCP Server 啟動：
#   python -m core.brain.mcp_server --workdir /your/project
#   或設定環境變數：BRAIN_WORKDIR=/your/project python -m core.brain.mcp_server

# ── Project Brain v3.0 ────────────────────────────────────────
# L1 工作記憶（Anthropic Memory Tool — 需要 SDK >= 0.74.0）
# anthropic>=0.74.0  # 已在核心依賴中

# L2 情節記憶（Graphiti 時序知識圖譜，選填）
# pip install graphiti-core falkordb
# graphiti-core>=0.3.0       # 時序知識圖譜引擎
# falkordb>=1.0.0             # 輕量 graph DB（推薦）

# 若使用 Neo4j（更完整）：
# neo4j>=5.0.0

# 安裝指令（最小 L2 配置）：
#   pip install graphiti-core falkordb
#   docker run -p 6379:6379 falkordb/falkordb

# ── Project Brain v4.0 ────────────────────────────────────────
# Web UI（知識圖譜視覺化）
# pip install flask flask-cors
# flask>=3.0.0
# flask-cors>=4.0.0

# LoRA 訓練（選填，需要 GPU）
# pip install unsloth  OR  pip install axolotl
